meta {
  name: GET /aggregated copy
  type: http
  seq: 2
}

get {
  url: {{url}}/transaction/aggregated?pivot=category
  body: none
  auth: bearer
}

params:query {
  pivot: category
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
