meta {
  name: GET /export
  type: http
  seq: 4
}

get {
  url: {{url}}/bulk-data/export
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
